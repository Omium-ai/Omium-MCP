# Base URL the MCP server uses for outbound calls to Omium.
# Default targets Kong inside the platform's Docker network.
OMIUM_API_BASE=http://kong:8000

# Bind address / port inside the container.
MCP_HOST=0.0.0.0
MCP_PORT=9100
